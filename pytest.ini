[pytest]
# Onde procurar os testes
testpaths = tests

# Mostra testes lentos (acima de 1s)
addopts =
    -v
    --tb=short
    --durations=5

# Ignora warnings do LangChain sobre deprecação
filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning
