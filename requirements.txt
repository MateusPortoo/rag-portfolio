# ── Core RAG pipeline ──────────────────────────────────────────────────────
langchain==0.3.25               # Orquestração do pipeline RAG
langchain-community==0.3.24     # Integrações: loaders, vector stores
langchain-groq==0.2.5           # Integração Groq API (LLM rápido e gratuito)

# ── Document parsing ────────────────────────────────────────────────────────
pypdf==5.6.0                    # Lê PDFs página a página
python-docx==1.1.2              # Lê arquivos .docx do Word

# ── Embeddings (local, sem API) ─────────────────────────────────────────────
sentence-transformers==4.1.0    # Modelo all-MiniLM-L6-v2 (384 dimensões)

# ── Vector database ─────────────────────────────────────────────────────────
chromadb==1.0.12                # Banco vetorial local com persistência em disco

# ── API (Fase 6+) ───────────────────────────────────────────────────────────
fastapi==0.115.9                # Framework web para a API REST
uvicorn[standard]==0.34.3       # Servidor ASGI que roda o FastAPI

# ── Frontend (Fase 8) ────────────────────────────────────────────────────────
streamlit==1.45.1               # Interface web em Python puro
requests==2.32.3                # HTTP client para chamar a FastAPI

# ── Testes (Fase 9) ─────────────────────────────────────────────────────────
pytest==8.4.0                   # Framework de testes
pytest-cov==6.1.0               # Relatório de cobertura de código
httpx==0.28.1                   # Necessário pelo TestClient do FastAPI

# ── Utilities ───────────────────────────────────────────────────────────────
python-dotenv==1.1.0            # Lê variáveis de ambiente do .env
groq==0.13.1                    # SDK oficial da Groq
